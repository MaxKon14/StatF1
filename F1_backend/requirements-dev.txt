# Инструменты разработки: тесты, линтеры, хуки.
# В прод-образ не попадают — с ГП-2, когда Dockerfile станет multi-stage.
# До этого момента Dockerfile ставит оба файла: иначе тесты не запустить в контейнере.

-r requirements.txt

# Тесты
pytest==9.1.1
pytest-django==4.13.0
pytest-cov==7.1.0

# Фабрики тестовых объектов и моки HTTP
factory_boy==3.3.3
respx==0.23.1

# Линтер, форматтер, хуки
ruff==0.15.10
pre-commit==4.5.1
